<source>
  @type tail
  tag "barito"
  path <%= @log_file_path %>
  <parse>
    @type none
  </parse>
</source>

<match barito>
  @type barito_vm

  application_secret <%= @app_secret %>
  produce_url <%= @produce_url %>
  <buffer>
    flush_mode immediate
  </buffer>
</match>